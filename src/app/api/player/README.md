# Deprecated Player API

All endpoints under `/api/player/*` are legacy and will be phased out.
New development should target the archive schema and server-side routes under `/api/archive/*`.

The `players` collection remains temporarily for migration compatibility only.
